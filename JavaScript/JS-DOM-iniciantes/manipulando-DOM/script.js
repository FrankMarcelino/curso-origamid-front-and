// Retorne o url da página atual utilizando o objeto window

const urlDoSite = window.location.href

console.log(urlDoSite)

// Seleciona o primeiro elemento da página que
// possua a classe ativo
 document.querySelector(".ativo")
// Retorne a linguagem do navegador

const linguagemNavegador = window.navigator.language

console.log(linguagemNavegador)

// Retorne a largura da janela 

let laguraJanela = window.innerWidth

console.log(laguraJanela)